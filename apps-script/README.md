# 구글 시트 + Apps Script 백엔드 설치 가이드

이 폴더의 파일들만으로 물류센터 업무관리 시스템 전체(화면 + 데이터베이스
+ 파일 저장소)가 구글 인프라 위에서 돌아갑니다. 별도 서버나 호스팅, DB
가입이 필요 없습니다 — Apps Script를 배포하면 나오는 웹 앱 주소 **하나**가
팀 전체가 접속하는 업무관리 시스템 주소가 됩니다 (화면도, 데이터 저장도
전부 그 주소 하나로 처리됩니다). 앱 파일을 개인 PC에 나눠줄 필요가
없습니다.

자세한 스크린샷 포함 설치 절차는 앱 안내에서 받은 "설치 매뉴얼" 페이지를
참고하세요. 이 문서는 파일 구성과 핵심 개념 요약입니다.

## 파일 구성

| 파일 | 역할 |
| --- | --- |
| `Schema.gs` | 시트 탭 이름·열 구성·JSON 인코딩 대상 필드 정의 |
| `Sheets.gs` | 시트를 "id로 찾는 테이블"처럼 다루는 범용 도우미 |
| `Code.gs` | 웹 앱 진입점(`doGet`) — 쿼리 파라미터가 없으면 `App.html`(앱 화면)을, 있으면 API 요청을 처리 |
| `Drive.gs` | 파일 업로드/삭제 (구글 드라이브) |
| `Setup.gs` | 설치 메뉴, 초기 시트 생성, 기존 데이터 가져오기 |
| `Import.html` | "기존 데이터 가져오기" 메뉴가 띄우는 붙여넣기 창 |
| `App.html` | 업무관리 시스템 화면 자체(빌드된 프론트엔드 한 파일) |

## 설치 요약

1. [sheets.google.com](https://sheets.google.com)에서 새 스프레드시트 생성
2. 확장 프로그램 → Apps Script
3. 기본 `Code.gs`를 지우고, 이 폴더의 7개 파일을 파일명까지 똑같이 만들어
   내용을 그대로 붙여넣기 (`Import.html`, `App.html`은 HTML 파일로
   만들어야 함 — 편집기에서 파일 추가 시 "HTML"을 선택)
4. 스프레드시트로 돌아가면 상단에 "물류센터 업무관리 설치" 메뉴가 생김
   (안 보이면 새로고침)
5. 메뉴 → "1. 초기 설정" 실행 → 권한 승인 → 시트 12개 자동 생성 +
   API 토큰 발급 + admin/blp00487 관리자 계정 자동 생성
6. (선택) 메뉴 → "2. 기존 데이터 가져오기" — 지금 쓰던 앱의 관리자 설정 →
   데이터(DB) 구조 → "전체 JSON 내보내기"로 받은 내용을 붙여넣으면 기존
   업무·사용자·카테고리 등을 그대로 옮길 수 있음. **가져오면 지금 시트에
   있는 데이터(5번에서 자동 생성된 admin 계정 포함)는 전부 지워지고 붙여넣은
   내용으로 교체됩니다** — 그러니 이 단계를 할 거면 5번 직후, 다른 데이터를
   만들기 전에 하는 게 좋습니다
7. Apps Script 편집기 → 배포 → 새 배포 → 웹 앱 → 액세스 권한 "전체" →
   배포 → 웹 앱 URL 복사
8. **그 주소를 그대로 브라우저로 열면 업무관리 시스템 화면이 뜹니다.**
   처음 열면 "데이터 연동 설정" 창이 자동으로 뜨고 주소도 이미 채워져
   있으니, 메뉴 → "3. API 토큰 다시 보기"에서 확인한 토큰만 입력하고
   "연결 테스트" → "저장하고 새로고침"
9. 이후로는 팀원들에게 **7번에서 복사한 웹 앱 주소만** 공유하면 됩니다.
   각자 그 주소로 접속해서(즐겨찾기 해두면 편함) 같은 방식으로 토큰
   한 번만 입력하면 됩니다.

## 데이터 구조

시트 12개가 프론트엔드 `src/lib/types.ts`의 각 타입과 1:1로 대응됩니다
(Teams, Centers, CategoryLarge, CategoryMedium, Boards, CustomFields,
Users, Tasks, ChecklistItems, LogEntries, Comments, Resources). 배열/객체
필드(예: `viewTeamIds`, `collaboratorIds`, `attachments`)는 셀에 JSON
문자열로 저장됩니다 — 시트에서 직접 값을 고칠 때는 이 점에 주의하세요.

## "기존 데이터 가져오기"는 추가가 아니라 교체입니다

가져오기를 실행하면 12개 시트를 전부 비운 뒤 붙여넣은 JSON으로 새로
채웁니다 (추가가 아닙니다). 백업에서 복원하거나, 다른 곳의 데이터를
한 번에 그대로 옮기는 용도에 맞춘 동작입니다. 지우지 않고 쌓이는
방식이었다면 admin 계정처럼 이미 있는 데이터가 매번 중복돼서 오히려
헷갈립니다. 지금 시트 내용을 보존하고 싶다면, 가져오기 전에 스프레드시트
전체를 사본으로 복사해두세요 (파일 → 사본 만들기).

## 인증에 대한 안내

이 백엔드는 요청마다 `API_TOKEN` 하나로만 인증합니다 (Apps Script 속성에
저장). 토큰과 웹 앱 URL을 아는 사람은 누구나 전체 데이터(비밀번호 해시
포함)를 읽고 쓸 수 있습니다. 사내에서만 URL·토큰을 공유하고, 외부에
노출되지 않도록 주의하세요. 이미 이 시스템의 로그인 자체가 "프로토타입
수준" 보안(클라이언트 SHA-256 해시 비교)이라는 점과 같은 맥락입니다 —
민감한 실제 인사·급여 데이터를 다루는 용도로는 적합하지 않습니다.

## 스크립트를 수정한 경우 재배포하는 방법

`apps-script/*.gs` 파일 내용을 바꿨다면, Apps Script 편집기에서 배포 →
배포 관리 → 연필(수정) 아이콘 → 버전: 새 버전 → 배포. 웹 앱 URL은 그대로
유지되므로 팀원들이 다시 설정할 필요는 없습니다.

## 통신 방식이 GET인 이유 (POST를 쓰지 않는 이유)

Apps Script 웹 앱의 `/exec` 주소는 실제 실행 서버로 302 리다이렉트됩니다.
브라우저 `fetch()` 표준(Fetch 스펙)은 POST 요청이 301/302 리다이렉트를
만나면 요청을 자동으로 GET으로 바꾸고 본문(body)을 버립니다 — 그 결과
실제로는 토큰도 요청 내용도 서버에 전혀 전달되지 않고, Apps Script 실행
기록(Executions)에는 `doPost`가 한 번도 찍히지 않고 `doGet`만 반복해서
찍히는 증상으로 나타납니다. 이 문제 때문에 프론트엔드(`gas-client.ts`)는
모든 요청을 GET으로 보내고, action/token/payload를 JSON으로 묶어
`?data=` 쿼리 파라미터 하나에 실어 보냅니다. `Code.gs`의 `doGet`이 이
파라미터를 파싱해서 처리합니다 (파라미터가 없으면 상태 확인 메시지만
보여줍니다). 첨부파일은 URL 길이 제한 때문에 작은 조각으로 나눠 여러 번의
GET 요청으로 보내고, 서버가 마지막 조각에서 전체를 이어붙여 드라이브에
저장합니다 (`uploadFileChunk` 액션, `Drive.gs`).

**이전에 이미 설치하신 분들은 다음 두 가지를 반영해주세요.**

1. `Code.gs`, `Drive.gs`를 이 저장소의 최신 내용으로 다시 덮어쓰기
   (GET 방식 통신 수정 사항)
2. `App.html` 파일을 새로 추가하기 — 이걸 추가하면 앱 파일을 따로
   나눠줄 필요 없이, 배포된 웹 앱 주소를 여는 것만으로 화면이 뜨게 됩니다
   (아래 "웹 앱 주소 하나로 화면까지 서빙하는 방법" 참고)

수정 후에는 배포 → 배포 관리 → 새 버전으로 재배포해주세요. 웹 앱 URL과
API 토큰은 그대로 쓰시면 됩니다.

## 웹 앱 주소 하나로 화면까지 서빙하는 방법

`Code.gs`의 `doGet`은 요청에 `data` 쿼리 파라미터가 없으면(=사람이 그냥
브라우저 주소창에 입력해서 열었을 때) `App.html`을 그대로 돌려줍니다.
이때 `ScriptApp.getService().getUrl()`로 알아낸 "이 배포의 안정적인 웹 앱
주소"를 `window.__DKE_BACKEND_URL__` 전역 변수로 화면에 심어주기 때문에,
프론트엔드가 처음 켜질 때 그 값을 읽어서 "데이터 연동 설정" 창의 URL
칸을 자동으로 채웁니다 — 여는 사람은 API 토큰만 입력하면 됩니다.
`App.html`은 `src/` 전체를 빌드한 결과물 한 파일이라 사람이 직접 편집하는
파일이 아닙니다 — 프론트엔드 코드를 고친 뒤에는 이 파일을 다시 빌드해서
통째로 교체해야 합니다.
